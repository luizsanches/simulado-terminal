using namespace std; // Usar os metodos padroes das bibliotecas

// Define a constante do pre-compilador para ser usada como parametro da funcao replica
#define REPETE 35

// Define a constante do pre-compilador para ser usada como cor de fundo padrao
#define COR_FUNDO "preto"

// Define a constante do pre-compilador para ser usada como opcao de sistema operacional
// (x = Unix/Linux) ou (w = Windows)
#define SO 'x'